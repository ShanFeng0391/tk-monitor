# ===== 混合部署模板（本地 API + Beat + Worker×24，云 RDS + Redis + OSS）=====
# 1. 复制为 .env.hybrid 并填写云资源连接信息
# 2. 初始化云数据库：.\scripts\init-hybrid-db.ps1
# 3. 启动：.\scripts\start-hybrid.ps1

LOCAL_MODE=false
SERVE_FRONTEND=true
APP_ENV=production
DEBUG=false

SECRET_KEY=请替换为至少32位随机字符串
JWT_SECRET_KEY=请替换为至少32位随机字符串
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=1440

DATA_DIR=data

ENABLE_API_DOCS=false
# CORS_ORIGINS=https://your.domain.com

# ---------- PostgreSQL（云 RDS）----------
# 异步（API / Worker）：postgresql+asyncpg://用户:密码@主机:5432/库名?ssl=require
DATABASE_URL=postgresql+asyncpg://tiktok_monitor:请填写密码@rm-xxx.pg.rds.aliyuncs.com:5432/tiktok_monitor?ssl=require
# 同步（Alembic / Celery 部分逻辑）：postgresql://用户:密码@主机:5432/库名?sslmode=require
DATABASE_URL_SYNC=postgresql://tiktok_monitor:请填写密码@rm-xxx.pg.rds.aliyuncs.com:5432/tiktok_monitor?sslmode=require

# ---------- Redis（云 Redis，DB0=Celery，DB1=代理池）----------
# 无 TLS：redis://:密码@主机:6379/0
# 有 TLS：rediss://:密码@主机:6379/0
REDIS_URL=rediss://:请填写密码@r-xxx.redis.rds.aliyuncs.com:6379/0
PROXY_POOL_REDIS_DB=1

# ---------- 对象存储（OSS / COS / TOS，S3 兼容）----------
MINIO_ENDPOINT=oss-cn-hangzhou.aliyuncs.com
MINIO_ACCESS_KEY=请填写RAM子账号AK
MINIO_SECRET_KEY=请填写RAM子账号SK
MINIO_BUCKET=tiktok-monitor-covers
MINIO_SECURE=true

# ---------- Worker / API ----------
CELERY_WORKER_CONCURRENCY=24

# 快照归档与 PG 备份
SNAPSHOT_RETENTION_DAYS=90
SNAPSHOT_PURGE_BATCH_SIZE=5000
POSTGRES_BACKUP_KEEP_LOCAL=7
POSTGRES_BACKUP_KEEP_REMOTE=14
POSTGRES_BACKUP_BUCKET_PREFIX=backups/postgres
# 本地 Beat 跑备份需 pg_dump；PG 在远程轻量/自建时建议 false，改在服务器 cron 备份
POSTGRES_BACKUP_ENABLED=false
API_UVICORN_WORKERS=1

SCRAPE_ALLOW_MOCK=false
SCRAPE_INTERVAL_MINUTES=1440
SCRAPE_REQUEST_DELAY_MIN=5
SCRAPE_REQUEST_DELAY_MAX=12
SCRAPE_MAX_RETRIES=3
SCRAPE_RETRY_INTERVAL_MINUTES=5
SCRAPE_PROXY_URL=

RECENT_VIDEO_DAYS=10
RECENT_VIDEO_UPDATE_DAYS=3
HISTORICAL_VIEW_THRESHOLD=100000
DAILY_HOT_VIEW_THRESHOLD=50000
DAILY_HOT_GROWTH_THRESHOLD=30
VIRAL_VIEW_THRESHOLD=100000

PROXY_POOL_ENABLED=true
PROXY_POOL_HEALTH_URL=https://www.tiktok.com
PROXY_POOL_BAD_TTL_SECONDS=1800
PROXY_POOL_MAX_FAIL_STREAK=3
PROXY_POOL_LOCAL_ENV_FALLBACK=false

SINGBOX_ENABLED=true
PROXY_GATEWAY_BASE_PORT=18080

DOUBAO_ENABLED=true
ARK_API_KEY=your-ark-api-key-here
ARK_BASE_URL=https://ark.cn-beijing.volces.com/api/v3
ARK_MODEL=doubao-1-5-vision-pro-32k-250115
RECOGNITION_AUTO_TRIGGER=false

TMDB_API_KEY=your-tmdb-api-key-here
BANGUMI_ENABLED=true
BANGUMI_ACCESS_TOKEN=
BANGUMI_USER_AGENT=tiktok-monitor/1.0 (https://github.com/local/tiktok-monitor)

ADMIN_USERNAME=admin
ADMIN_PASSWORD=请设置强密码
ADMIN_EMAIL=admin@example.com
